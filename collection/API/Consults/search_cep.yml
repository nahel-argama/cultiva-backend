info:
  name: Search CEP
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/consult/cep/:cep"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: cep
      value: "13506000"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
