info:
  name: Signup Metadata
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/v1/auth/signup/metadata"
  headers:
    - name: Accept
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
